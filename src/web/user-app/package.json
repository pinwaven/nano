{
  "name": "nano-user-app",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "sync:coach": "node scripts/sync-coach-from-miniapp.mjs",
    "predev": "npm run sync:coach",
    "prebuild": "npm run sync:coach",
    "dev": "vite --port 5178 --open",
    "build": "vite build",
    "preview": "vite preview",
    "test": "node --test tests/*.test.mjs"
  },
  "dependencies": {
    "axios": "^1.6.0",
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.2.0",
    "vite": "^5.0.0",
    "vite-plugin-commonjs": "^0.10.4",
    "postcss": "^8.5.16"
  }
}
