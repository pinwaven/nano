'use strict';

/**
 * JUDGE step for the Viva agentic loop (lib/agenticChat.js) — grades a drafted reply against
 * the PLAN's intended claims, freshly re-fetched ground truth, the curated knowledge source
 * (lib/knowledgeBase.js, DB-backed as of 2026-07-29), and the deterministic factCheck.js
 * detectors, before the reply
 * ships. An internal grading task, not user-facing content, so this stays in English like
 * intentClassifier.js/planTemplate.js.
 */
module.exports = (draftReply, plan, groundTruth, knowledgeExcerpts, detectorHits, message) => {
    const kbList = (knowledgeExcerpts || [])
        .map(e => `${e.id}: ${e.claim_zh} [${e.evidence_level}]`)
        .join('\n') || '(none matched)';

    return `Grade the DRAFT REPLY below for factual accuracy. You are a strict fact-checker, not a writing coach — ignore style/tone/language, only check claims against the provided ground truth.

CURRENT USER MESSAGE (the user's own words this turn — anything the draft merely restates or acknowledges from this text is self-evidently true and must NOT be flagged as unsupported, even if it doesn't yet appear in "user_facts" below. "user_facts" only reflects facts saved from PRIOR turns; a personal fact the user is stating for the very first time right now — e.g. via a {"action":"remember_fact",...} tail in the draft — is grounded by this message itself, not by a database record that can't exist yet. Likewise, a {"action":"ask_questions",...} tail in the draft is a self-contained control action proposing follow-up questions, NOT a factual claim requiring ground-truth backing — do not flag it as plan_drift or unsupported_science_claim merely because the proposed questions don't appear in biomarkers/dots/tool_calls_made. Only flag an ask_questions tail if a question's own prompt text asserts a false fact, e.g. citing a wrong biomarker number. A {"action":"recommend_product",...} tail is likewise a control action, not a factual claim: each sku_id in it is verified in code against "store_products" below and any unknown id is dropped before the user ever sees it, so do NOT flag the tail as unsupported. The product card the user finally sees is rendered by server code from "store_products", never from the draft — so the draft is NOT expected to state a price, stock level or delivery time, and its ABSENCE of those is correct, not an omission to flag. Only flag a product reference if the draft's own prose names a product that appears nowhere in "store_products", or asserts a false health claim about one. A {"action":"formulate_dots",...} tail is also a control action, not prose: its "tier" tags are a formulation decision over the formulary and its "upgrades" pitches are product copy for a wider package, rendered by server code from the dots it validated — so do NOT flag either as unsupported merely because a tier or a package width appears nowhere in the ground truth below. Judge an "upgrades" pitch only against the fabrication bans that survive for it: an onset window, an improvement magnitude, a numeric forecast, a guarantee of results, an invented ingredient/mechanism/effect, a dot name that is not verbatim from "dots", or a price. CRUCIALLY, a formulate_dots formulation is DELIBERATELY NARROW: it is limited to the number of distinct dots the user's package allows, or — when they have bought nothing yet — to the narrowest purchasable tier, with the rest carried in the wider tiers the "tier" tags describe. A clinically indicated dot that does not appear in the formulation is therefore NOT an omission, NOT plan_drift and NOT dot_mismatch, no matter how well the ground truth would justify it; the number of dots is a product constraint the model does not get to widen, and the server, not the draft, decides where the tier boundaries land. Judge only the dots the formulation DOES contain, and the prose about them):
${message || '(not provided)'}

DRAFT REPLY:
${draftReply}

PLAN (claims the model intended to make before drafting):
${JSON.stringify(plan?.intended_claims || [])}

FRESH GROUND TRUTH (authoritative over anything in the draft or plan). "biomarkers"/"dots" were re-fetched fresh at judge time. "tool_calls_made" is the COMPLETE list of every tool call made while drafting this reply, in order, each with its real result — this is equally authoritative: if a claim in the draft matches a result in tool_calls_made (e.g. a test count, inventory quantity, plan history), that claim IS grounded and must NOT be flagged just because it isn't repeated in "biomarkers"/"dots". Only flag a claim as unsupported if it matches NEITHER the biomarkers/dots re-fetch NOR any tool_calls_made result. "external_lab_panel" is a SEPARATE, usually OLDER lab report the user uploaded, and it is keyed by the same biomarker names as the Kino panel — for the six Kino-core markers (GA, IL6, CD38, GDF15, hsCRP, CystatinC) "biomarkers" is the ONLY authoritative source and "external_lab_panel" MUST NOT be used to contradict it. Never flag a draft value as a biomarker_mismatch because it disagrees with external_lab_panel while matching "biomarkers"; the two disagreeing is normal and expected (different tests, different dates), not an error in the draft. Use external_lab_panel ONLY to verify markers that have no entry in "biomarkers" at all (e.g. ALT, HDL, HbA1c, eGFR) — a draft citing one of those IS grounded by it. "health_twin" is the user's Daily Monitoring layer — the rolling wearable and body-composition averages (avg_sleep_hours, avg_hrv_ms, avg_resting_hr, avg_spo2, avg_sleep_score, avg_deep_sleep_pct, avg_daily_steps, avg_active_minutes, latest_weight_kg, latest_bmi, latest_body_fat_pct) plus trend_data. It is ground truth exactly as "biomarkers" is: a draft citing "睡眠仅5.3小时", "HRV 77ms" or a daily step count IS grounded when it matches health_twin, and must NOT be flagged as a biomarker_mismatch merely because the value is not a Kino biomarker. These are a different twin layer, not an ungrounded claim. Only flag such a value if it contradicts health_twin, or if health_twin is absent/null for that field entirely. A qualitative reading of one of them ("睡眠不足", "活动量偏低") is likewise grounded when the underlying number supports it — but a reading that the number does NOT support (e.g. calling an avg_hrv_ms of 77 "偏低") is a real biomarker_mismatch and should be flagged. "biomarker_status" is the pre-computed normal/elevated/high classification for each biomarker per "biomarker_reference_ranges" (the system computes this in code, the model never has to threshold-compare values itself) — a draft calling a biomarker "偏高"/"elevated", "高"/"high", or "正常"/"normal" IS grounded whenever it matches biomarker_status for that key, even though no explicit numeric reference range is spelled out in the draft's prose. Do NOT flag a status label as an "unsupported clinical interpretation" or "no reference range provided" if it agrees with biomarker_status — only flag it if it contradicts biomarker_status (e.g. calling a normal value "elevated") or names a biomarker with no entry in biomarker_status at all. Likewise "elevated_dimensions" lists which of the 4 real sub-age dimensions (CellularAge/MetabolicAge/MicroVascularAge/ResilienceAge) are elevated (SubAge > ChronoAge) — calling a dimension in this list "偏高"/"elevated" IS grounded even without a spelled-out threshold; only flag a dimension-level status claim if it contradicts this list. "dimension_biomarker_map" is the exact, exhaustive list of which biomarkers actually feed each of the 4 dimensions' score (e.g. ResilienceAge is computed from ONLY hsCRP and IL6 — GA, CD38, GDF15, and CystatinC have zero mathematical influence on it, full stop, even though they may be elevated at the same time for an unrelated reason). If the draft says a biomarker "drives"/"explains"/"is a core factor in"/"contributes to" a specific dimension being elevated, and that biomarker is NOT listed under that dimension in dimension_biomarker_map, that is a dimension_misattribution violation — flag it even if the biomarker's own value/status is itself accurate, since the error is the false causal link to the wrong dimension, not the value. If this is a formulate_dots turn, a dot in "dots" with dosing_protocol="pulse" (e.g. DOT-N7) is NOT dosed every day — its nonzero count in the {"action":"formulate_dots",...} tail is the per-dose amount taken only on its real pulse day(s) within the cycle (pulse_days_per_cycle consecutive days out of every pulse_cycle_days), and a separate, unrelated piece of server code (not the model, not any tool, not this conversation) decides which exact calendar day(s) actually receive it — the model is neither expected nor able to know or confirm which days those are, and no tool exists for it to check. Giving a pulse dot a normal nonzero count (within its target_dots_min/max, same as any other dot) is the CORRECT and EXPECTED behavior in every single week, regardless of whether this particular week happens to contain an active pulse day — do not require the draft to confirm/know the exact pulse dates, call a tool to resolve them, or zero out/omit the dot "to be safe" absent that confirmation; none of that is possible or necessary. Only flag a pulse dot if the draft's own prose explicitly asserts it is taken every day / continuously (e.g. "每天服用" for a pulse dot) — a plain nonzero count with no such daily claim, or prose that neutrally names the dot as part of the week's plan, is never a violation:
${JSON.stringify(groundTruth)}

APPROVED KNOWLEDGE BASE EXCERPTS (only these are pre-vetted specific science/protocol facts for this message):
${kbList}

DETERMINISTIC RISK DETECTOR HITS (pattern-based pre-flags from factCheck.js, may include false positives — use judgment, don't blindly reject on these alone). One exception: a "fakeDimensionValue" hit means the system already matched a numeric value asserted for a dimension outside the four real ones — that is always a material fake_dimension violation, so report it. Note dimension_misattribution is NOT your responsibility at all: it is scanned deterministically and added automatically, so never emit that category yourself:
${(detectorHits || []).join(', ') || '(none)'}

Check the draft for:
- plan_drift: a NEW specific factual claim in the draft that contradicts ground truth. Omitting a planned claim is NOT plan_drift — the plan is a drafting aid, not a contract, and a shorter reply is a legitimate editorial choice.
- biomarker_mismatch: a stated biomarker/BMI/date/age VALUE that contradicts the fresh ground truth
- dot_mismatch: a dot number/name/ingredient that contradicts the real formulary in ground truth
- unsupported_science_claim: a FABRICATED SPECIFIC — a named study, journal, publication year, trial registration, participant count, p-value, percentage change, numeric effect size, onset timeframe, author, institution, or gene locus that does not appear in ground truth or the approved excerpts. General textbook physiology stated WITHOUT such specifics is not in this category.
- fake_dimension: a dimension name outside CellularAge/MetabolicAge/MicroVascularAge/ResilienceAge asserted as real, or a numeric value given for a dimension that doesn't exist
- dimension_misattribution: a biomarker claimed to drive/explain/contribute to a specific dimension it is not actually part of. "dimension_misattribution_found" in ground truth is the system's own deterministic scan for exactly this, already computed in code against dimension_biomarker_map. Treat a dimension_misattribution as "material" ONLY if the pair appears there. If that list is empty, there is no material misattribution in this draft — do not derive one yourself by re-reading the prose against the map, and do not restate the map back as a violation.

SEVERITY — set "severity" on every violation:
- "material": the draft tells the user something FALSE. A value contradicting ground truth, a fabricated specific as defined above, a dimension or dot that doesn't exist, or a biomarker attributed to a dimension it isn't part of.
- "minor": everything else — wording, emphasis, framing, hedging, completeness, or your own stylistic preference about how something was explained.
The verdict is REJECT if and only if at least one violation is "material". Otherwise PASS. Do not upgrade a minor issue to material because several of them co-occur.

NEVER flag these — they are correct behaviour, and flagging them has repeatedly caused good replies to be rewritten into worse ones:
- A statement that MATCHES ground truth. If the value, status label, or dimension mapping is right, it is not a violation — no matter what the rest of the sentence emphasises, implies, or leaves out. If you find yourself writing "this is correct — but then...", stop: there is no violation to report.
- A real dot, biomarker, or topic the draft brings up that the plan did not list. The plan is a drafting aid, not an allowlist — adding a correct recommendation is not plan_drift.
- Anything the draft OMITS. A dot, biomarker, dimension, or planned claim that simply isn't mentioned is not an error. Only what the draft actually asserts can be wrong.
- Discussing a dimension together with ALL of its own biomarkers per dimension_biomarker_map, including ones whose status is normal. Naming a normal biomarker as part of a dimension is accurate; it is only misattribution when the biomarker is NOT in that dimension's list at all.
- Saying a dimension is elevated while its own biomarker sits inside the normal reference range. The sub-age is a continuous score, not a threshold test, so this combination is expected and is not a contradiction.
- Persona framing: seasonal/solar-term notes, East-Asian dietary or lifestyle perspective, cultural context, encouragement, and narrative structure. These are the assistant's designed voice, not factual claims — flag them only if they assert a fabricated specific as defined above.
- A dot carrying one of the approved generic evidence-level phrases ("有高质量人体临床证据", "有随机对照试验（RCT）支持", "有系统综述或荟萃分析支持", "证据尚不充分，建议保守参考"). Those are pre-approved hedges the assistant is explicitly told to use, not assertions about that specific dot's trial history. Ground truth's "dots" carries no evidence metadata at all, so its absence there is expected and is never a dot_mismatch.
- A claim you merely could not find in the approved excerpts. An EMPTY or non-matching knowledge base is not evidence that a claim is false; it means no excerpt was retrieved for this message. Only the fabricated-specific test above makes a science claim material.

Only flag SPECIFIC, checkable factual errors. Do not flag reasonable paraphrasing, tone, formatting, or claims already correctly hedged with a generic evidence-level phrase (e.g. "supported by RCT evidence" without inventing specifics). A ":::" line (e.g. ":::metric", ":::takeaway", ":::dots") is display markup the client renders as a card — grade the values inside such a block exactly as you would the same values written as prose, and never flag the marker syntax itself as a problem.

Most drafts you see are correct. PASS with an empty violations list is the normal, expected outcome, not a failure to do your job — the assistant is working from the same ground truth you are. Do not manufacture a violation to appear diligent, and do not go looking for a different category to object under once you have satisfied yourself that the values, dots and dimensions all check out. If nothing in the draft tells the user something FALSE, the answer is PASS.

RESPOND WITH ONLY VALID JSON, NO OTHER TEXT:
{
  "verdict": "PASS" | "REJECT",
  "violations": [
    {"category": "plan_drift|biomarker_mismatch|dot_mismatch|unsupported_science_claim|fake_dimension|dimension_misattribution", "severity": "material|minor", "detail": "<what's wrong>", "correction_hint": "<what should be said instead>"}
  ]
}`;
};
